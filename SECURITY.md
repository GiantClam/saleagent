# 安全指南

## 敏感信息处理

本项目遵循安全最佳实践，确保敏感信息不会被提交到代码仓库。

### ✅ 已保护的内容

1. **环境变量文件**：所有 `.env` 文件已被 `.gitignore` 忽略
2. **API Keys**：所有 API 密钥通过环境变量配置，不硬编码在代码中
3. **数据库凭证**：数据库连接信息仅通过环境变量提供
4. **私钥文件**：所有 `.key`、`.pem`、`.cert` 等密钥文件已被忽略

### 📝 示例配置文件

项目包含以下示例配置文件（**可以安全提交**）：

- `apps/agent/.env.example` - 后端环境变量模板
- `apps/web/.env.example` - 前端环境变量模板

这些文件仅包含占位符，不包含真实的敏感信息。

### 🔒 需要配置的环境变量

#### 后端（apps/agent/.env）

```bash
# 复制示例文件
cp apps/agent/.env.example apps/agent/.env

# 然后编辑 .env 文件，填入真实的配置值
```

#### 前端（apps/web/.env.local）

```bash
# 复制示例文件
cp apps/web/.env.example apps/web/.env.local

# 然后编辑 .env.local 文件，填入真实的配置值
```

### ⚠️ 注意事项

1. **永远不要提交 `.env` 文件**：这些文件包含真实的敏感信息
2. **检查 Git 状态**：提交前运行 `git status` 确保没有意外添加敏感文件
3. **使用环境变量**：所有敏感配置都应通过环境变量提供
4. **代码审查**：在代码审查时检查是否有硬编码的密钥或密码

### 🔍 检查清单

在提交代码前，请确认：

- [ ] 没有 `.env` 文件被添加到 Git
- [ ] 代码中没有硬编码的 API keys
- [ ] 代码中没有硬编码的密码
- [ ] 代码中没有硬编码的数据库连接字符串
- [ ] 文档中的示例值都是占位符（如 `sk-...`、`eyJ...`）

### 🛠️ 如果意外提交了敏感信息

如果意外提交了敏感信息到 Git 仓库：

1. **立即轮换密钥**：更换所有泄露的 API keys 和密码
2. **从 Git 历史中删除**：使用 `git filter-branch` 或 `git filter-repo` 工具
3. **通知团队成员**：告知团队成员需要更新配置
4. **检查访问日志**：检查相关服务的访问日志，确认是否有未授权访问

### 📚 参考资源

- [GitHub 安全最佳实践](https://docs.github.com/en/code-security)
- [OWASP 安全指南](https://owasp.org/)
- [环境变量管理](https://12factor.net/config)

